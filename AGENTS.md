1. Ломай обратную совместимость если у этого есть любые рациональные соображения, посколько нет объектов, привязанных к интерфейсам проекта.
2. Для backend предпочитай FastAPI + SQLAlchemy + Postgres.
3. Упакивывай всё в docker compose + Makefile, позволяющий задеплоить проект на целевой машине простым `make up`.